import { ComingSoon } from '@/components/coming-soon';

export default function ReportsScreen() {
  return (
    <ComingSoon
      icon="chart.bar.fill"
      accent="#B0E4CC"
      title="reports"
      description="Exportable season reports — matches, attendance and finances — are on their way."
    />
  );
}
